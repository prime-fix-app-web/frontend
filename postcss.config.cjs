// Local PostCSS override to disable any parent/global config.
// This prevents Vite from loading G:\postcss.config.js (or other parent configs).
// No plugins are applied.
module.exports = {
  plugins: []
}

